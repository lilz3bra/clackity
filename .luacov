return {
  include = {
    -- Only report on files inside your plugin's lua directory
    "lua/clackity/.*",
    "lua/clackity/.*/.*"
  },
  exclude = {
    -- Ignore the tests themselves and the downloaded dependencies
    "tests/.*",
    "%.tests/.*",
    -- Ignore the presentation layer and orchestrator
    "lua/clackity/init%.*",
    -- "ui/init%.*",
    -- "core/init%.*",
    -- "lua/clackity/input/init.lua"
    "core/definitions%.*",
    "core/state%.*",
    "healt%.*"
  }
}
